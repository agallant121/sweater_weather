# require 'rails_helper'
#
# RSpec.describe 'reverse geocode' do
#   it 'can display city given a lat and long' do
#     reverse_city = Antipode.new('Hong Kong').
#
#     expect(reverse_city.get_coords.keys.count).to eq(2)
#     expect(reverse_city.get_coords.keys.first).to eq(:lat)
#     expect(reverse_city.get_coords.keys.last).to eq(:long)
#   end
# end
